# XamlRuntime

Native XAML runtime with the OpenGL ES renderer and the Windows markup compiler.

The package exports CMake targets through `find_package(XamlRuntime CONFIG REQUIRED)` and supports Windows x64 plus Android arm64.